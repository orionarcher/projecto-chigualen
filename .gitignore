# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.direnv/

# Raw + intermediate data — large and regenerable via the pipeline
Chigualen/data/raw/
Chigualen/data/clean/
Chigualen/*.csv
Chigualen/*.pdf
Chigualen/*.zip

# But DO commit the runtime outputs the app reads
!Chigualen/data/out/

# Streamlit
.streamlit/secrets.toml

# Pre-repair snapshots written by scripts/09_repair_outputs.py
Chigualen/data/out/*.pre-repair
