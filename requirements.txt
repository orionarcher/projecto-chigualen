# App runtime. This is what a host installs to serve app/app.py — keep it
# minimal so deploys are fast and have little to go wrong.
#
# The pipeline needs more (a PDF engine); see requirements-pipeline.txt. Nothing
# under app/ imports it, so it is deliberately not here.
streamlit>=1.36
pandas>=2.0
